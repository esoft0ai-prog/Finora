const createTriggerNotification=jest.fn(async()=>{}),cancelTriggerNotification=jest.fn(async()=>{}),getTriggerNotificationIds=jest.fn(async()=>[]),createChannel=jest.fn(async()=> 'finora-finance'),requestPermission=jest.fn(async()=>({})),getNotificationSettings=jest.fn(async()=>({authorizationStatus:1,android:{alarm:1}}));jest.mock('@notifee/react-native',()=>({__esModule:true,default:{createChannel,createTriggerNotification,cancelTriggerNotification,getTriggerNotificationIds,requestPermission,getNotificationSettings,displayNotification:jest.fn()},AndroidImportance:{HIGH:4},AndroidNotificationSetting:{DISABLED:0},RepeatFrequency:{DAILY:1,WEEKLY:2},TriggerType:{TIMESTAMP:0}}));
const execute=jest.fn(async()=>({})),one=jest.fn(async()=>undefined),rows=jest.fn(async()=>[]);jest.mock('../src/database/db',()=>({execute,one,rows}));jest.mock('../src/database/preferences',()=>({loadPreferences:jest.fn(async()=>({notificationEnabled:true,notificationCategories:{debt:true,budget:true,savings:true,bill:true,custom:true}}))}));
import {scheduleLocalReminder} from '../src/services/notificationService';
describe('notification scheduling',()=>{test('schedules future reminder and persists it',async()=>{const future=new Date(Date.now()+3600000).toISOString();await scheduleLocalReminder({title:'Bill',message:'Due soon',scheduledAt:future,category:'bill'});expect(createTriggerNotification).toHaveBeenCalled();expect(execute).toHaveBeenCalledWith(expect.stringContaining('INSERT OR REPLACE INTO reminders'),expect.any(Array));});test('rejects past reminder',async()=>{await expect(scheduleLocalReminder({title:'Old',message:'Old',scheduledAt:'2020-01-01T00:00:00.000Z',category:'custom'})).rejects.toThrow(/future/i);});});
