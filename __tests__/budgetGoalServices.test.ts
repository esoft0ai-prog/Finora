const mockExecute=jest.fn(async()=>({})),mockOne=jest.fn(),mockRows=jest.fn(async()=>[]);jest.mock('../src/database/db',()=>({mockExecute,mockOne,mockRows}));
const mockAudit=jest.fn(async()=>{});jest.mock('../src/services/auditService',()=>({mockAudit}));
jest.mock('../src/services/notificationService',()=>({displayBudgetWarning:jest.fn(async()=>{})}));
import {createBudget,updateBudget} from '../src/services/budgetService';import {createGoal,updateGoal,contributeToGoal} from '../src/services/goalService';
describe('budget and goal services',()=>{beforeEach(()=>jest.clearAllMocks());test('creates and updates budget',async()=>{const b={name:'Food',periodType:'monthly',amount:50000,startDate:'2026-09-01',endDate:'2026-09-30',currency:'NGN'};await createBudget(b);await updateBudget('b1',b);expect(mockExecute).toHaveBeenCalledWith(expect.stringContaining('INSERT INTO budgets'),expect.any(Array));expect(mockExecute).toHaveBeenCalledWith(expect.stringContaining('UPDATE budgets'),expect.any(Array));});test('creates and updates goal',async()=>{const g={name:'Laptop',targetAmount:800000,currentAmount:250000,deadline:'2026-12-20',currency:'NGN'};await createGoal(g);await updateGoal('g1',g);expect(mockExecute).toHaveBeenCalledWith(expect.stringContaining('INSERT INTO savings_goals'),expect.any(Array));});test('caps contribution at target',async()=>{mockOne.mockResolvedValueOnce({target_amount:100000,current_amount:90000});await contributeToGoal('g1',20000);expect(mockExecute).toHaveBeenCalledWith(expect.stringContaining('UPDATE savings_goals'),expect.arrayContaining([100000]));});});
