const execute=jest.fn(async()=>({})),one=jest.fn();jest.mock('../src/database/db',()=>({execute,one}));
const audit=jest.fn(async()=>{});jest.mock('../src/services/auditService',()=>({audit}));
const createTransaction=jest.fn(async()=> 'tx-pay');jest.mock('../src/services/transactionService',()=>({createTransaction}));
const scheduleDebtReminders=jest.fn(async()=>{});jest.mock('../src/services/notificationService',()=>({scheduleDebtReminders}));
import {createDebt,recordDebtPayment,reconcileDebtStatuses} from '../src/services/debtService';
describe('debt service',()=>{beforeEach(()=>jest.clearAllMocks());test('creates debt and reminders',async()=>{await createDebt({lenderName:'Bank',principalAmount:100000,interestRate:10,dueDate:'2026-10-25',startDate:'2026-09-25',minimumPayment:20000,currency:'NGN'});expect(execute).toHaveBeenCalledWith(expect.stringContaining('INSERT INTO debts'),expect.any(Array));expect(scheduleDebtReminders).toHaveBeenCalled();});test('records repayment and advances due date',async()=>{one.mockResolvedValueOnce({id:'d1',lender_name:'Bank',current_balance:100000,currency:'NGN',due_date:'2026-10-25',payment_frequency:'monthly'});await recordDebtPayment({debtId:'d1',amount:20000,accountId:'a1',paidAt:'2026-10-25T10:00:00.000Z'});expect(createTransaction).toHaveBeenCalled();expect(execute).toHaveBeenCalledWith(expect.stringContaining('UPDATE debts SET current_balance'),expect.arrayContaining([80000,'2026-11-25']));});test('reconciles overdue states',async()=>{await reconcileDebtStatuses();expect(execute).toHaveBeenCalledTimes(2);});});
