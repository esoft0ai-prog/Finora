import React from 'react';import {Pressable,StyleSheet} from 'react-native';import {Card} from './Card';import {Txt} from './Typography';import {formatMoney} from '../utils/currency';import {usePalette} from '../theme/theme';
export const MetricCard=({label,value,currency='NGN',tone='normal',onPress}:{label:string;value:number;currency?:string;tone?:'normal'|'good'|'bad';onPress?:()=>void})=>{const p=usePalette();return <Pressable onPress={onPress} style={styles.item}><Card style={styles.card}><Txt muted style={styles.label}>{label}</Txt><Txt style={[styles.value,{color:tone==='bad'?p.danger:tone==='good'?p.success:p.text}]}>{formatMoney(value,currency)}</Txt></Card></Pressable>};
const styles=StyleSheet.create({item:{width:'48%'},card:{minHeight:106,justifyContent:'space-between'},label:{fontSize:12},value:{fontWeight:'800',fontSize:20}});
