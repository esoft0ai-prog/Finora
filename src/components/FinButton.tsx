import React from 'react';import {Pressable,StyleSheet,ActivityIndicator} from 'react-native';import {Txt} from './Typography';import {usePalette} from '../theme/theme';
export const FinButton=({title,onPress,variant='primary',disabled=false,busy=false}:{title:string;onPress:()=>void;variant?:'primary'|'secondary'|'danger'|'ghost';disabled?:boolean;busy?:boolean})=>{const p=usePalette();const bg=variant==='primary'?p.primary:variant==='danger'?p.danger:variant==='secondary'?p.surface2:'transparent';const fg=variant==='primary'?'#04120e':variant==='danger'?'#fff':p.text;return <Pressable accessibilityRole="button" accessibilityLabel={title} disabled={disabled||busy} onPress={onPress} style={({pressed})=>[styles.b,{backgroundColor:bg,borderColor:variant==='ghost'?p.border:bg,opacity:disabled?.45:pressed?.78:1}]}>{busy?<ActivityIndicator color={fg}/>:<Txt style={{color:fg,fontWeight:'800'}}>{title}</Txt>}</Pressable>};
const styles=StyleSheet.create({b:{minHeight:48,paddingHorizontal:18,borderRadius:14,borderWidth:1,alignItems:'center',justifyContent:'center'}});
