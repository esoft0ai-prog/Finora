import React from 'react';import {ScrollView,Pressable,StyleSheet} from 'react-native';import {Txt} from './Typography';import {usePalette} from '../theme/theme';
export const ChoiceChips=<T extends string>({items,value,onChange}:{items:{label:string;value:T}[];value:T;onChange:(v:T)=>void})=>{const p=usePalette();return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.row}>{items.map(i=><Pressable key={i.value} onPress={()=>onChange(i.value)} style={[styles.chip,{backgroundColor:value===i.value?p.primaryDim:p.surface2,borderColor:value===i.value?p.primary:p.border}]}><Txt style={{fontWeight:'700',color:value===i.value?p.primary:p.text}}>{i.label}</Txt></Pressable>)}</ScrollView>};
const styles=StyleSheet.create({row:{gap:8},chip:{borderWidth:1,borderRadius:999,paddingHorizontal:13,paddingVertical:9}});
