import React from 'react';import {View,StyleSheet} from 'react-native';import {Txt} from './Typography';import {usePalette} from '../theme/theme';
export const MiniBars=({data}:{data:{label:string;value:number}[]})=>{const p=usePalette();const max=Math.max(1,...data.map(x=>x.value));return <View style={styles.box}>{data.map(x=><View key={x.label} style={styles.row}><Txt muted style={styles.label}>{x.label}</Txt><View style={[styles.track,{backgroundColor:p.surface2}]}><View style={[styles.fill,{backgroundColor:p.primary,width:`${Math.max(2,(x.value/max)*100)}%`}]}/></View><Txt style={styles.num}>{Math.round(x.value).toLocaleString()}</Txt></View>)}</View>};
const styles=StyleSheet.create({box:{gap:10},row:{flexDirection:'row',alignItems:'center',gap:8},label:{width:76,fontSize:12},track:{flex:1,height:9,borderRadius:20,overflow:'hidden'},fill:{height:'100%'},num:{width:70,textAlign:'right',fontSize:12}});
