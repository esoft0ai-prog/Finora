import React,{useCallback,useState} from 'react';import {useFocusEffect} from '@react-navigation/native';import {Screen} from '../components/Screen';import {H1,H2,Txt} from '../components/Typography';import {Card} from '../components/Card';import {listNotifications} from '../database/repositories';
export default function NotificationHistoryScreen(){const [items,setItems]=useState<any[]>([]);useFocusEffect(useCallback(()=>{listNotifications().then(setItems)},[]));return <Screen><H1>Notification History</H1>{items.map(n=><Card key={n.id}><H2>{n.title}</H2><Txt>{n.body}</Txt><Txt muted>{new Date(n.deliveredAt).toLocaleString()} · {n.category}</Txt></Card>)}{!items.length?<Txt muted>No locally recorded notification history yet.</Txt>:null}</Screen>}
