import {create} from 'zustand';import type {Account,Budget,Category,DashboardMetrics,Debt,Preferences,Reminder,SavingsGoal,Transaction} from '../types/domain';import {DEFAULT_PREFERENCES,loadPreferences,savePreferences} from '../database/preferences';import {listAccounts,listBudgets,listCategories,listDebts,listGoals,listReminders,listTransactions} from '../database/repositories';import {getDashboardMetrics} from '../services/analyticsService';
type State={ready:boolean;busy:boolean;error:string|null;preferences:Preferences;accounts:Account[];categories:Category[];transactions:Transaction[];budgets:Budget[];debts:Debt[];goals:SavingsGoal[];reminders:Reminder[];dashboard:DashboardMetrics|null;refresh:()=>Promise<void>;setPreferences:(p:Partial<Preferences>)=>Promise<void>;setReady:(v:boolean)=>void;setBusy:(v:boolean)=>void;setError:(v:string|null)=>void;};
export const useAppStore=create<State>((set,get)=>({ready:false,busy:false,error:null,preferences:DEFAULT_PREFERENCES,accounts:[],categories:[],transactions:[],budgets:[],debts:[],goals:[],reminders:[],dashboard:null,setReady:v=>set({ready:v}),setBusy:v=>set({busy:v}),setError:v=>set({error:v}),refresh:async()=>{set({busy:true,error:null});try{const [preferences,accounts,categories,transactions,budgets,debts,goals,reminders,dashboard]=await Promise.all([loadPreferences(),listAccounts(),listCategories(),listTransactions(),listBudgets(),listDebts(),listGoals(),listReminders(),getDashboardMetrics()]);set({preferences,accounts,categories,transactions,budgets,debts,goals,reminders,dashboard});}catch(e){set({error:e instanceof Error?e.message:'Failed to load data.'});}finally{set({busy:false});}},setPreferences:async p=>{const next={...get().preferences,...p};await savePreferences(next);set({preferences:next});}}));
